import {StyleSheet} from "react-native"

export default unitStyles = StyleSheet.create({
    unitEndContainer: {
        flex: 1,
        justifyContent:'flex-end',
    },
    unitCenterContainer: {
        justifyContent:'center',
    }
    })